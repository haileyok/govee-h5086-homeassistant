# pytest-homeassistant-custom-component pins the rest of the HA test stack
# (and Home Assistant itself). Recent versions (0.13.110+) require Python
# 3.12; older versions support 3.11. We allow either by leaving the floor
# at the 3.11-compatible release. CI uses Python 3.12 and picks up the
# latest pinning.
pytest>=8.0
pytest-asyncio>=0.23
pytest-homeassistant-custom-component>=0.13.100
ruff>=0.6
cryptography>=41.0
bleak>=0.22
bleak-retry-connector>=4.0
# pyserial is a transitive dependency of homeassistant.components.usb that
# isn't always pulled into the test venv automatically.
pyserial>=3.5
